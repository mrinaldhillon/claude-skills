{
  "name": "scaffold",
  "description": "Project-enabled scaffolding layer: milestone/dev/skill-maintenance workflows, the ADR + milestone commands and the /scaffold:milestone-run runner, context-checkpoint & config-guard hooks, a determinism auditor, and references/ for by-hand project setup. Enable per-repo in .claude/settings.json; requires the core plugin and assumes the docs/ + .context/ layout.",
  "version": "0.2.0",
  "author": {
    "name": "Mrinal Dhillon"
  },
  "keywords": [
    "milestone",
    "workflow",
    "adr",
    "milestone-runner",
    "checkpoint",
    "scaffolding"
  ],
  "dependencies": ["core"]
}
