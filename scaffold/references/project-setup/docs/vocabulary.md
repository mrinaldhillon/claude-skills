# Vocabulary

Terms that are easy to misread, each with the misunderstanding it prevents. Add a
term the first time someone (human or model) gets it wrong.

| Term | Means | NOT (the misread it prevents) |
|---|---|---|
| `<example>` | <the precise definition in this project> | <the plausible-but-wrong reading> |

<PLACEHOLDER: replace the example row. Good candidates: a word your domain overloads,
an identifier that looks like a standard one but isn't, a unit/encoding convention.>
